{
    "watch": ["src/"],
    "ext": ".ts",
    "ignore": [
        "*.test.ts",
        "./dist"
    ],
    "exec": "ts-node .",
    "events": {
      "restart": "npm run pretty && npm run prestart && echo '===>>> DONE BUILD'",
      "crash": "kill-port 3000"
    }
}
